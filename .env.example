# Optional: enables the LLM summary layer in the verification agent.
# Without it, the verifier still returns web-search sources (no key required).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5
